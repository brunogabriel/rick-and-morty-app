object TestDependencies {
    const val junit = "junit:junit:${TestVersions.junit}"
    const val androidxJunit = "androidx.test.ext:junit:${TestVersions.androidxJunit}"
    const val espresso = "androidx.test.espresso:espresso-core:${TestVersions.espresso}"
    const val truth = "com.google.truth:truth:${TestVersions.truth}"
    const val coreTesting = "androidx.arch.core:core-testing:${TestVersions.coreTesting}"
    const val coroutinesTest = "org.jetbrains.kotlinx:kotlinx-coroutines-test:${TestVersions.coroutinesTest}"
    const val mockk = "io.mockk:mockk:${TestVersions.mockk}"
}